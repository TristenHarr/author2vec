[package]
name = "corpus"
version = "0.1.0"
edition.workspace = true
default-run = "corpus"

[dependencies]
fastembed = "5"
regex = "1"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1"
shared = { path = "../shared" }
toml = "0.8"
ureq = "2"
