[package]
name = "shared"
version = "0.1.0"
edition.workspace = true

# Pure Rust: no native-only deps so this compiles for both host and wasm32.
[dependencies]
serde = { version = "1", features = ["derive"] }
